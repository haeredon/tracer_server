#include "EventController.h"
#include "Event.h"

void EventController::handle(event_t* event) {
    // std::vector<const AbstractRule&> entityRules = this->entityService.getEntityRules(event->entity, event->type);
    // AbstractEventParser& parser = this->parserFactory.getParser(event->type);

    // if()

    // parse event

    // get entity id

    // get entity rules for event type and appliccation

    // run rules on data. no need to optimize for now. return type should be some internal event data object. not the Event class

    // save it to storage    
}